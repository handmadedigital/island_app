import { capacityLeft } from '../../../helpers/capacity-left';
import { module, test } from 'qunit';

module('Unit | Helper | capacity left');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = capacityLeft(42);
  assert.ok(result);
});
