import { orderTotal } from '../../../helpers/order-total';
import { module, test } from 'qunit';

module('Unit | Helper | order total');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = orderTotal(42);
  assert.ok(result);
});
